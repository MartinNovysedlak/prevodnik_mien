{
  "name": "prevodnik-mien",
  "version": "1.0.0",
  "description": "Prevodník mien - mobilná aplikácia",
  "private": true,
  "scripts": {
    "build:web": "echo \"Web assets are static, nothing to build\"",
    "sync": "cap sync android",
    "open:android": "cap open android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/haptics": "^6.0.3",
    "@capacitor/network": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
